import styled from "styled-components";

export const ButtonWrapper = styled.button`
    all: unset;
    font-size: 2rem;
    background-color: #00B0CE;
    color: #fff;
    height: 4rem;
    aspect-ratio: 1/1;
    display: grid;
    place-items: center;
    cursor: pointer;
`
